const authControllers = require("./auth.controller");
const todoControllers = require("./todo.controller");
const uploadControllers = require("./upload.controller");

module.exports = { authControllers, todoControllers, uploadControllers };
